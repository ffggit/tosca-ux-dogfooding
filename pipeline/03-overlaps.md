# UX Issues — Overlaps (Cross-Designer)

_Generated from 02-merged.md. Each row is a group of issues that share the same root UX problem across two or more designers._
_Description: LLM-generated synthesis capturing what, why, and full context — no information lost._
_Merged IDs: the original issue IDs from the per-designer files that were merged into this group._
_Column structure identical to per-designer files._

| ID | Merged IDs | Environment | Feature | Screen / page / component | Flow / step | Issue type | Severity | Description | Fix hypothesis |
|---|---|---|---|---|---|---|---|---|---|
| OVL-001 | FG-010, AZ-002, DL-009 | Dev / Staging | Launcher / Inventory | Launcher install prompt — first encounter | Encountering Launcher for the first time mid-flow | Help & guidance | Major | Three designers independently hit the same moment: the Launcher (or a browser extension it installs) appears without any prior warning, system requirements, install time estimate, or explanation of what it does or why it is needed. FG-010 describes Edge silently installing an extension with no user consent — the first signal being Edge's own alarming security alert. AZ-002 describes a first-time user hitting an unexpected download dialog mid-flow with no context. DL-009 describes the browser extension prompt appearing mid-scan, breaking the flow — the user had assumed extension install was bundled with Launcher. The common root: Launcher and its dependencies are injected into the user journey without preparation, and the product does not brief the user before asking them to install software on their machine. | Surface a brief, upfront briefing — what Launcher is, what it installs, why it is required — before the user reaches any Launcher-triggered prompt. Treat it as a mandatory pre-step with clear system requirements. |
| OVL-002 | FG-005, AZ-003, AZ-004 | Dev | Launcher / Platform | Launcher installer — OS detection | Attempting to install or download Launcher on macOS | Error prevention, Help & guidance | Critical | Two designers on macOS hit the same hard blocker: the product has no OS detection and no macOS warning. AZ-003 describes the system automatically downloading a Windows .exe on macOS, which fails with error -10661 with no explanation. AZ-004 confirms the Mac Launcher simply does not exist — the user is completely blocked from recording ARA test cases with no alternative and no warning. FG-005 documents the broader pattern: the entire DI setup flow assumes Windows, with no mention that a Windows VM or agent is required anywhere in the product or documentation. Together these three issues describe a systemic platform-assumption failure: Tosca Cloud silently blocks macOS users at multiple entry points without acknowledging that a Windows environment is required. | Add OS detection at download time and show a clear, upfront message for macOS users: Launcher requires Windows. Provide guidance on how to proceed (VM setup, agent configuration) before the user encounters a silent failure. |
| OVL-003 | FG-008, FG-009, FG-011, DL-005 | Dev / Staging | Launcher | Launcher — failure states | Running a test → Launcher opens or attempts to install components | Feedback | Major | Four issues across two designers describe Launcher or its components failing without surfacing a meaningful, actionable error to the user. FG-008: TBox is quarantined by antivirus — UI shows only "Component executable not found"; the actual cause is buried in a log file. FG-009: browser extension install fails with a raw error code (ServiceError::UPDATE_DISABLED) that gives no context about the VM configuration or how to fix it. FG-011: TricentisHtmlEngineExtensionHelper.exe is blocked by antivirus — the file is named correctly in the error but no Defender exclusion path, documentation link, or workaround is suggested. DL-005: an already-installed Launcher fails silently with no error at all, requiring the user to manually uninstall and reinstall. The shared root: Launcher's failure modes consistently present symptoms rather than causes, and never provide a recovery path. | For each Launcher failure state, surface the actual cause (not just the symptom), and include at minimum one actionable next step — whether that is an exclusion path, a documentation link, or a suggestion to reinstall. |
| OVL-004 | JA-011, GT-020, DL-029 | Test / Prod / Staging | Run / Playlist | Playlist / run trigger — no feedback after action | Triggering a run (playlist or test execution) | Feedback | Major | Three designers describe the same dead moment after clicking Run: the UI gives no signal that anything has started. JA-011 notes the only hint was the cursor occasionally changing; the Launcher modal only appeared much later. GT-020 describes clicking multiple times and silently creating duplicate runs with no way to stop them. DL-029 flags the button itself as ambiguous — labeled "Test run" rather than "Run", so users are not even certain what they triggered. The shared root: the run action has no immediate acknowledgment, leaving users uncertain whether the click registered, and in GT-020's case, inadvertently creating multiple parallel executions. | Acknowledge the run action immediately on click — a loading state, a banner, or navigation to the run view. Prevent duplicate triggers by disabling the button or providing inline feedback while a run is in progress. |
| OVL-005 | GT-022, JA-025, GT-018 | Prod / Test | Run / Test execution | Run results — locating failure details | Reviewing results after a completed run | Feedback, Efficiency | Major | Three issues describe the same gap in how run results are surfaced after execution completes. GT-022 states that after a playlist run, only a generic "failed" status is shown — understanding which tests failed and why requires at least 3 clicks. JA-025 notes that troubleshooting requires knowing to click a red icon, which is not discoverable to first-time users. GT-018 describes the only end-of-run signal as the execution bar losing its orange color — no pass/fail summary, no navigation to results. The shared root: the product does not surface results proactively; users must know where to look and how to navigate there. In On Prem, the scratchbook opened automatically. | At run completion, surface a result summary in context (pass/fail count, link to failed tests) without requiring additional navigation. |
| OVL-006 | GT-014, DL-028 | Prod / Staging | Builder / Playlist editor | Test case editor / Playlist editor — no autosave | Navigating away from or working within the editor | Efficiency, Error prevention | Major | Two designers explicitly call out missing autosave as a source of frustration and anxiety. GT-014 describes frequent, disruptive save dialogs when navigating away from unsaved test cases — a pattern common in iterative work. DL-028 describes constant anxiety about losing unsaved changes across the entire session, requiring repeated manual saves. Both note that autosave is standard in modern SaaS and its absence feels outdated. The root is the same across both the test case editor and the playlist editor: no autosave, high risk of data loss, and a disruptive user experience during iterative workflows. | Implement autosave in the test case editor and playlist editor. Where autosave is not immediately feasible, provide a persistent "unsaved changes" indicator and remove the blocking save dialog on navigation. |
| OVL-007 | GT-009, DL-016 | Prod / Staging | Scanner / XScan | Module save — workspace and naming | Completing a scan / saving a module | Control & freedom, Efficiency | Major | Two designers independently describe the same problem: users have no control over where a module is saved or what it is named at the time of scanning. GT-009 reports scanned modules landing in the wrong workspace because there is no workspace selector in the scan flow. DL-016 reports module names being auto-generated with no option to name them before scanning begins, making them difficult to identify later. DL-015 adds that the save name and location are easy to overlook entirely. Together these issues describe a scan flow that does not ask the user to make two of the most consequential decisions — where to save and what to call — before or during the process. | Add a naming and destination step to the scan flow before or at the point of saving, so users can assign a meaningful name and select the correct workspace without post-hoc renaming. |
| OVL-008 | FG-002, FG-006, AMN-022 | Dev / Staging | Data Integrity / Test execution | Error messages — cause vs. symptom | Encountering a failure during DI connection, test run, or debugging | Feedback | Major | Three issues across two designers describe error messages that name a symptom or an internal detail instead of the actual cause, leaving users unable to diagnose the problem. FG-002: "SQL command execution timed out" gives no indication of whether the problem is the query, the connection, or the server. FG-006: Test Connection failure gives no diagnostic info — does not indicate whether the cause is a wrong connection string, inaccessible file path, or network issue. AMN-022: error "couldn't find tab" appeared when the real issue was button identification — the error actively misdirected the user. The shared root: error messages in multiple parts of the product prioritize technical detail over user-actionable information, making self-diagnosis impossible. | Rewrite failure messages to state the probable cause in plain language and include at least one recovery suggestion. Where the cause is genuinely unknown, say so — and point to where more information can be found (log, documentation). |
| OVL-009 | AMN-004, AMN-006, AMN-007, AMN-012, GT-011, GT-012, GT-013 | Staging / Prod | Scanner / XScan | XScan — complex control scanning | Scanning non-trivial UI controls (dropdowns, floating menus, tables, grid buttons, drag-and-drop) | Help & guidance, Efficiency | Critical | Seven issues across two designers describe the same class of XScan failure: the tool cannot reliably capture non-trivial UI controls, and users have no in-product guidance when it fails. AMN-004: cannot scan floating menus or dropdown controls — not solvable even with full DOM mode. AMN-007: impossible to scan or create drag-and-drop operations without external guides. AMN-012: scanning tables extremely difficult with no path forward. GT-011: XScan fails silently on Chrome incognito windows — no error, no explanation. GT-012: XScan captures the DIV container instead of individual dropdown options when the dropdown is closed. GT-013: buttons inside grid rows (download, delete) are captured incorrectly — XScan selects the parent container instead of the target. AMN-006: re-scanning does not refresh the XScan view; users must close and restart. The shared root: XScan's interaction model breaks down on the controls users are most likely to need in real applications, and fails silently with no fallback guidance. | Surface inline, contextual help for common XScan failure patterns (floating elements, dropdowns, tables, grid cells). Where XScan cannot capture a control, show an explicit error or fallback suggestion rather than failing silently. Improve state refresh on rescan so users do not have to close and restart. |
| OVL-010 | FG-001, FG-003, FG-005, JA-001 | Dev / Test | Data Integrity / Builder | DI setup and template flow — missing guidance | Full DI connection setup / template creation flow | Help & guidance | Major | Four issues across two designers describe the same systemic absence: the product provides no in-context guidance for flows that have significant hidden prerequisites. FG-001: no guidance that a working database connection with accessible tables is required before the DI module can be used. FG-003: no indication that the SQLite file path must point to the agent machine — Mac users hit a silent failure with no explanation. FG-005: the entire DI connection flow assumes Windows with no mention of OS requirements anywhere. JA-001: the template → test sheet → XL → instantiation flow cannot be completed self-served; the user had to abandon the product and consult documentation. The shared root: both the DI connection flow and the template flow rely on knowledge that exists only outside the product, and provide no in-product guidance to bridge that gap. | Add contextual guidance at the critical entry points of each flow: what is required to proceed, what the prerequisites are, and what to do if the user does not meet them. Inline tooltips, empty-state explanations, or a brief guided first-run are all viable approaches. |
| OVL-011 | FG-004, AZ-005, AZ-006, JA-015, GT-020, DL-027 | Dev / Staging / Test / Prod | Various | Missing confirmation feedback after key actions | Completing save, link, download, record step, or run trigger | Feedback | Major | Six issues across four designers describe the same gap: the system provides no acknowledgment after a key user-initiated action. FG-004: "Use caching database" checkbox gives no feedback on whether it connected or failed. AZ-005: downloading Launcher gives no cloud-side confirmation — re-clicking downloads again. AZ-006: during ARA recording, no confirmation is shown after each recorded step — users cannot tell if actions are registering. JA-015: linking a test sheet produces no confirmation message. GT-020: clicking Run produces no feedback (see also OVL-004). DL-027: saving a playlist freezes all CTAs for ~5 seconds with no loading indicator. The shared root: across features and designers, the product consistently omits acknowledgment of completed actions, leaving users to guess whether the system received their input. | Implement consistent, immediate feedback for all key action completions — success toasts, state changes, or loading indicators at minimum. Define a product-wide feedback pattern and apply it uniformly across features. |
| OVL-012 | DL-003, GT-004, AMN-001 | Staging / Prod | Workspace management | Workspace — post-creation state | Creating a new workspace | Consistency, Feedback, Efficiency | Major | Three designers describe a broken post-creation experience for workspaces. DL-003: the creator is not automatically added to the workspace they just created — they have to hunt for the setting and add themselves manually. GT-004: creating a private workspace and clicking "Go to workspace" results in an access denied error even with admin rights — the workspace must be made public to access it, with no warning. AMN-001: after creating a workspace, the user is redirected to the full list of workspaces instead of landing inside the one they just created. The shared root: workspace creation does not complete to a usable state — the user either cannot access what they created, lands in the wrong place, or is not given the access they expect as the creator. | After workspace creation: auto-add the creator as a member, navigate directly into the new workspace, and surface any access or visibility constraints before they block the user. |
| OVL-013 | JA-004, JA-010, JA-016 | Test | Builder | Template instantiation — flow and feedback | Saving and instantiating a template | Feedback, Consistency, Efficiency | Major | Three issues from the same designer describe the template instantiation flow as broken across multiple steps. JA-004: instantiation reports success but produces no results — the success message is misleading and sends the user into repeated troubleshooting. JA-010: the Instantiate button disappears after saving and only reappears when the template row is explicitly clicked, even when it is the only item — users lose access to the action they just prepared to use. JA-016: Save and Instantiate are separate steps, with Instantiate disabled until after saving — a tooltip tells the user to save first, which breaks flow. The shared root: the instantiation step is fragile, inconsistently available, and does not report its actual outcome — making it the highest-friction step in the template workflow. | Combine Save + Instantiate into a single action where possible. Auto-select the template row after saving so Instantiate is always available. Do not show a success message when zero instances are generated — instead surface the actual failure reason. |
| OVL-014 | JA-005, JA-006 | Test | Builder / TDM | Template — terminology and linking clarity | Linking a test sheet and inspecting generated instances | Consistency, Language & clarity | Major | Two issues from the same designer describe a terminology collision that makes the template workflow confusing at two key moments. JA-005: the user could not verify what was linked to the template and later discovered the wrong sheet was linked — the linked sheet name is not confirmed or previewed after linking. JA-006: generated template instances are labelled "Test Sheets" by default — the same term used for the source data structure in the Prepare area. This collision makes it impossible to distinguish the output from the input by label alone. The shared root: the product reuses the same term for two different concepts (source data sheet and generated output artifact), and does not confirm the current link state so users can verify they linked correctly. | Rename generated artifacts to something distinct from source test sheets (e.g. "Test case instances"). After linking a test sheet, show the sheet name and a data preview in the details panel so users can confirm the correct sheet is linked. |
| OVL-015 | DL-023, DL-024 | Staging | Inventory > Test case editor | Standard objects vs User assets tabs | Searching for a module in the test case builder | Language & clarity, Consistency, Efficiency | Major | Two issues from the same designer describe adjacent problems in the module search experience. DL-023: the distinction between Standard objects and User assets is unclear, and User assets is not the default tab — leading the user to search in the wrong place repeatedly. DL-024: even when searching in User assets, standard modules also appear in results, adding noise and confusion. The shared root: the module search architecture separates Standard and User assets in a way that does not match the user's mental model, and the search results do not respect that separation consistently. | Make User assets the default tab for module search, or unify the search across both with clear origin labelling. Ensure search results are scoped to the selected tab. |
| OVL-016 | DL-015, DL-016 | Staging | Inventory > Module editor | Module editor — naming and save location | Saving a scanned module | Feedback, Control & freedom, Efficiency | Major | Two closely related issues from the same designer describe a module naming and saving problem. DL-015: the save name and location are easy to overlook at the end of a scan, causing significant time loss later when trying to identify and reuse the module. DL-016: module names are always auto-generated with no option to name them before scanning begins. Together they describe the same root problem from two angles: users have no meaningful control over module identity at the time of creation, and the default output is hard to find and distinguish later. Grouped here alongside OVL-007 (cross-designer variant of the same issue). | Prompt for a module name and destination workspace before starting the scan, or make naming the first required step of the save flow. |
| OVL-017 | AMN-010, AMN-021 | Staging | Modules / Builder | Module page / Test case editor — no back navigation | Editing a module or test case | Control & freedom | Minor | Two issues from the same designer describe the same navigation dead-end: once inside a detail view (module page or test case editor), there is no affordance to return to the parent list. In both cases the user is trapped in the detail context with no obvious escape. | Add a "Back to [list]" breadcrumb or navigation control to both the module detail page and the test case editor. |
